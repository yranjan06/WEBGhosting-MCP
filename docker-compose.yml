version: '3.8'

services:
  webmcp:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AI_API_KEY=${AI_API_KEY}
      - AI_MODEL=${AI_MODEL:-gpt-4o}
      - BROWSER_HEADLESS=true
    command: ["--port=8080"]
    restart: unless-stopped
